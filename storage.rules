rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Signature files are uploaded by authorized Next.js Server Actions and
    // streamed through an authenticated admin route. Direct browser access to
    // every Storage object remains denied.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
