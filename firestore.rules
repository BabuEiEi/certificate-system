rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The application accesses Firestore only through authenticated Next.js
    // server code using the Admin SDK. Client SDK access is denied by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
