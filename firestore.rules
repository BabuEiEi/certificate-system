rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Admin data, including events and signers, is accessed only through
    // authenticated Next.js server code using the Admin SDK.
    // Client SDK access remains denied by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
